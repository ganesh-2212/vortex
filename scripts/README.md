# Scripts

This directory is intended for convenience scripts, utility tasks, and automation helpers used across the Revenue Sentinel repository.

Development servers for frontend and backend are run independently during development:
- **Backend:** Run using Uvicorn within the backend virtual environment.
- **Frontend:** Run using Vite development commands.
