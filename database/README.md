# Database and Migration Foundation

This directory houses the PostgreSQL database schema migrations.

## Directory Structure

- `migrations/`: Directory where database migration scripts (e.g. SQL files or Alembic migrations) will be stored.

## Future Plans

The complete application schema migrations (including `merchants`, `payments`, `incidents`, `interventions`, and `audit_logs`) will be introduced in milestone **F03 Database foundation**.
